# Data Wrangling

Raw corpus cleaning is handled by the `wrangle` CLI tool in the core package.

```bash
uv run wrangle --input <dir> --output <dir>
```

See the root [README.md](../README.md) for full CLI reference.
